[package]
name = "norse-token"
version = "0.1.0"
description = "NorseCoin Solana Program - Viking-themed token with staking, NFTs, and presale"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "norse_token"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.0"
anchor-spl = "0.30.0"
